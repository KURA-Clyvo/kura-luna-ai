# === Core ===
oracledb==2.4.1
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.4.0

# === Mensageria ===
twilio==9.3.2

# === IA / Visão Computacional ===
ultralytics==8.3.0
opencv-python-headless==4.10.0.84
Pillow==10.4.0
torch==2.4.1
torchvision==0.19.1

# === CLI / Job ===
typer==0.12.5
# Pinado abaixo de 8.2 — Click 8.2.0 removeu CliRunner(mix_stderr=...), usado em
# tests/unit/cli/test_main.py. typer==0.12.5 só exige click>=8.0.0, então um
# install limpo (ex.: CI) resolve o click mais novo disponível e quebra a coleta
# dos testes de CLI.
click==8.1.8
APScheduler==3.10.4

# === Web / HTTP (v2.0) ===
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-multipart==0.0.9
